#include <iostream>

#include "Header.h"

int main()
{
	const std::string fileName("data.txt");
	const int numberCount = 50;
	const int maxNumberValue = 10;

	
	//createFileWithRandomNumbers(fileName, numberCount, maxNumberValue);
	//std::cout << isFileContainsSortedArray(fileName);
	polyphaseSort(",xc");
	return 0;
}